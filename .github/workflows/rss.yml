name: RSS Sync
on:
  push:
    branches: [main]
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  rss-job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Dependencies
        run: npm install

      - name: Fetch 人民网-政治
        continue-on-error: true
        run: node scripts/fetch-rss.js "https://plink.anyfeeder.com/people/politics" "feeds/people-politics.json"

      - name: Fetch 地球知识局
        continue-on-error: true
        run: node scripts/fetch-rss.js "https://plink.anyfeeder.com/weixin/diqiuzhishiju" "feeds/diqiuzhishiju.json"

      - name: Auto-Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自动更新 RSS 存档"
          file_pattern: "feeds/*"
