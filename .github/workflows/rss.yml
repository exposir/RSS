name: RSS Sync
on:
  push:
    branches: [main]
  schedule:
    - cron: "*/15 * * * *" # 每 15 分钟自动运行
  workflow_dispatch: # 支持手动触发

jobs:
  rss-job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Fetch 人民网-政治
        continue-on-error: true
        uses: Promptly-Technologies-LLC/rss-fetch-action@v2
        with:
          feed_url: "https://plink.anyfeeder.com/people/politics"
          file_path: "feeds/people-politics.json"

      - name: Fetch 36氪
        continue-on-error: true
        uses: Promptly-Technologies-LLC/rss-fetch-action@v2
        with:
          feed_url: "https://plink.anyfeeder.com/36kr/news/latest"
          file_path: "feeds/36kr.json"

      - name: Fetch IT之家
        continue-on-error: true
        uses: Promptly-Technologies-LLC/rss-fetch-action@v2
        with:
          feed_url: "https://plink.anyfeeder.com/ithome/rss"
          file_path: "feeds/ithome.json"

      - name: Auto-Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自动更新 RSS"
          file_pattern: "feeds/*"
