name: RSS Sync
on:
  push:
    branches: [main]
  schedule:
    - cron: "*/15 * * * *" # 每 15 分钟自动运行
  workflow_dispatch: # 支持手动触发

jobs:
  rss-job:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须开，否则机器人没法 Push 更新
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Fetch 人民网-政治
        continue-on-error: true
        uses: nahuelhds/rss-fetch-action@v0.0.10
        with:
          feed_urls: "https://plink.anyfeeder.com/people/politics"
          destination_folder: "feeds"

      - name: Fetch 36氪
        continue-on-error: true
        uses: nahuelhds/rss-fetch-action@v0.0.10
        with:
          feed_urls: "https://plink.anyfeeder.com/36kr/news/latest"
          destination_folder: "feeds"

      - name: Fetch IT之家
        continue-on-error: true
        uses: nahuelhds/rss-fetch-action@v0.0.10
        with:
          feed_urls: "https://plink.anyfeeder.com/ithome/rss"
          destination_folder: "feeds"

      - name: Auto-Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自动更新 RSS"
          file_pattern: "feeds/*"
